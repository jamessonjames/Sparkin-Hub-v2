/**
 * useAutoScheduler is disabled to respect user sovereignty over due dates.
 * Dates set by users or via drag-and-drop are persistent and never rescheduled automatically in the background.
 */
export function useAutoScheduler() {
  // Disabled: due dates are determined strictly by user actions and manual scheduling.
}
